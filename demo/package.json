{
  "name": "playtonight-demo",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Génère une vidéo de démo TikTok de PlayTonight (enregistrement auto + voix off + montage ffmpeg).",
  "scripts": {
    "record": "node demo.mjs",
    "build": "node build-video.mjs",
    "make": "node demo.mjs && node build-video.mjs",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
